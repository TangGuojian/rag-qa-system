# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
*.egg
.venv/
venv/
.pytest_cache/
*.db
*.db-shm
*.db-wal
*.db-journal
*.sqlite3

# Environment
.env
.env.*
*.env.local
!.env.example

# 本地数据库运维脚本（含本机密码，不纳入版本管理）
reset_pwd.sql
setup_db.ps1
reset_mysql_admin.ps1
start_mysql_skip.bat

# Node
node_modules/
dist/
.turbo/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Uploads
uploads/

# 向量库数据（本机索引，体积大且可由脚本重建）
chroma_data/
chromadb_data/

# 例外：前端构建产物需要入库。
# 为了让使用者 clone 后不必安装 Node.js 就能直接打开界面，后端会直接托管该目录。
# 重新构建后请一并提交：cd frontend && npm run build
!frontend/dist/

# 本仓库自身的克隆（例如把 GitHub 上的版本 clone 到项目目录里做对照测试）。
# 它是嵌套的 git 仓库，git 只会存一个空指针，没有任何意义，必须排除。
rag-qa-system/
