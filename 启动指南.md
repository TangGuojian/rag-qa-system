# 智能问答系统 — 启动指南

## 前置依赖

| 组件 | 版本 | 说明 |
|------|------|------|
| Python | 3.11+ | 已安装 |
| Node.js | 20.18.0 | 已安装 |
| MySQL 8.0 | 8.0.12 | 已安装，密码见 `backend/.env` |
| Neo4j | 3.5.5 | 社区版，已配置 |
| Chrome 浏览器 | — | 访问前端页面 |

## 0. 配置环境变量

```powershell
cd backend
copy .env.example .env
```

然后编辑 `backend/.env`，填入你自己的数据库密码与 LLM API Key。
**`.env` 不会被提交到版本库**，请勿把真实密钥写进任何其他文件。

## 1. 启动 MySQL

MySQL80 已注册为 Windows 服务，在命令提示符中运行：

```powershell
net start MySQL80
```

验证连接（`-p` 后面不写密码，回车后交互式输入）：

```powershell
& "C:\Program Files\MySQL\MySQL Server 8.0\bin\mysql.exe" -u root -p -e "SHOW DATABASES;"
```

## 2. 启动 Neo4j

```powershell
cd "<你的 Neo4j 安装目录>"
.\bin\neo4j.bat console
```

打开浏览器访问 `http://localhost:7474`，连接信息：

- URI：`bolt://localhost:7687`
- 用户名：`neo4j`
- 密码：见 `backend/.env` 中的 `NEO4J_PASSWORD`

## 3. 启动后端 API 服务

```powershell
cd backend
python -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
```

验证：

```powershell
curl http://localhost:8000/health
# 返回: {"status":"ok"}
```

## 4. 启动前端开发服务器

```powershell
cd frontend
npx vite --host 0.0.0.0
```

浏览器访问 `http://localhost:5173`

## 5. 登录

首次使用需调用初始化接口创建管理员：

```powershell
curl -X POST http://localhost:8000/api/v1/auth/init
```

| 角色 | 用户名 | 密码 |
|------|--------|------|
| 管理员 | `admin` | `admin123` |

> ⚠️ **这是开发环境的默认账号，部署到公网前务必修改。**
> 建议将 `/api/v1/auth/init` 限制为仅开发环境可用，或改为从环境变量读取初始密码。

## 6. 数据重导入

如需重新导入知识库文档：

```powershell
cd backend
python batch_import.py
```

> 文档目录需在 `batch_import.py` 的 `DATA_DIRS` 中配置为本机实际路径。

如需重新构建知识图谱：

```powershell
cd backend
python build_graph.py
```

## 7. 项目结构

```
rag-qa-system\
├── backend\                  # FastAPI 后端
│   ├── app\
│   │   ├── api\              # 9 个 API 路由模块
│   │   ├── core\             # JWT 认证、配置、依赖
│   │   ├── db\               # MySQL、Neo4j、ChromaDB 连接
│   │   ├── models\           # 7 个 SQLAlchemy 数据模型
│   │   ├── rag\              # RAG 引擎（核心）
│   │   │   ├── embedding.py       # 文本向量化
│   │   │   ├── retriever.py       # ChromaDB 向量检索
│   │   │   ├── graph_retriever.py # Neo4j 图谱检索
│   │   │   ├── llm.py             # LLM 生成（Qwen）
│   │   │   └── engine.py          # 混合检索引擎
│   │   ├── schemas\          # Pydantic 请求/响应模型
│   │   └── services\         # 业务逻辑层
│   ├── tests\                # pytest 测试用例
│   ├── chroma_data\          # ChromaDB 持久化数据（不纳入版本管理）
│   ├── .env.example          # 环境变量模板
│   ├── batch_import.py       # 文档批量导入脚本
│   └── build_graph.py        # 知识图谱构建脚本
└── frontend\                 # Vue 3 前端
    ├── src\
    │   ├── api\              # Axios API 封装
    │   ├── views\            # 7 个页面组件
    │   ├── store\            # Pinia 状态管理
    │   └── router\           # 路由配置
    └── .env.example          # 前端环境变量模板
```

## 8. API 端点

| 路径 | 方法 | 说明 |
|------|------|------|
| `/api/v1/auth/login` | POST | 用户登录 |
| `/api/v1/auth/init` | POST | 初始化管理员 |
| `/api/v1/knowledge-bases` | GET/POST | 知识库列表/创建 |
| `/api/v1/documents/upload` | POST | 文档上传 |
| `/api/v1/qa/ask` | POST | 智能问答 |
| `/api/v1/qa/feedback` | POST | 问答反馈 |
| `/api/v1/history` | GET | 问答历史 |
| `/api/v1/graph/data` | GET | 知识图谱数据 |
| `/api/v1/users` | GET/POST | 用户管理 |
| `/api/v1/config` | GET/PUT | 系统配置 |
| `/api/v1/dashboard/*` | GET | 仪表盘数据 |
