# 智能问答系统 —— 需求说明书

## 1. 项目概述

### 1.1 项目背景

随着企业数字化转型的深入，内部知识资产的沉淀与管理成为提升组织效率的关键。当前企业内部知识分散于各类文档中，员工获取信息依赖人工查询、经验传递等方式，效率低下且难以保证准确性。

本项目旨在构建一套基于 RAG（Retrieval-Augmented Generation）架构的智能问答系统，向企业用户提供基于私有知识库的精准问答服务，实现知识的快速检索与智能回答。

### 1.2 项目目标

- 构建企业级私有知识库，支持多知识库管理
- 基于大语言模型实现智能问答，提升知识获取效率
- 构建知识图谱，实现知识的结构化关联
- 提供完善的后台管理能力，支撑系统运营

---

## 2. 用户角色与权限

### 2.1 角色定义

| 角色 | 说明 | 主要权限 |
|------|------|---------|
| 管理员 | 系统运维管理角色 | 知识库管理、用户管理、工作台、问答历史、系统配置、知识图谱管理 |
| 普通用户 | 知识问答使用角色 | 智能问答、问答历史 |

### 2.2 权限矩阵

| 功能模块 | 管理员 | 普通用户 |
|---------|:------:|:--------:|
| 工作台（仪表盘） | ✓ | ✗ |
| 知识库管理 | ✓ | ✗ |
| 知识图谱管理 | ✓ | ✗ |
| 用户管理 | ✓ | ✗ |
| 系统配置 | ✓ | ✗ |
| 智能问答 | ✓ | ✓ |
| 问答历史 | ✓ | ✓ |

---

## 3. 功能需求

### 3.1 工作台（管理员仪表盘）

管理员登录后默认进入工作台，展示系统运行概况。

**功能列表：**

| 模块 | 功能描述 |
|------|---------|
| 知识库概览 | 展示各知识库的文档总数、已解析/向量化数量、图谱节点数与关系数 |
| 问答统计 | 展示总问答次数、活跃用户数、每日/周/月问答趋势图 |
| 系统运行状态 | 展示存储使用量、API 调用次数与频率、系统资源使用情况 |
| 热点推荐 | 展示高频提问词云或热门问题 Top-N 列表 |

### 3.2 知识库管理

支持多知识库的创建、管理与文档维护。

#### 3.2.1 知识库基础管理

- **创建/编辑/删除知识库**：支持创建多个知识库，可设置名称与描述
- **知识库列表**：展示所有知识库，支持搜索与筛选
- **知识库统计**：展示各知识库的文档数量、向量数量、图谱节点数

#### 3.2.2 文档管理

| 功能 | 详细描述 |
|------|---------|
| 文档上传 | 支持 PDF（.pdf）和 Word（.docx）格式，单文件最大 1024MB |
| 文档标签 | 支持为文档添加标签（如"财务"、"技术"、"人力"等），便于分类管理 |
| 文档列表 | 展示已上传文档，支持按知识库/标签/上传时间等维度筛选搜索 |
| 处理状态跟踪 | 文档上传后展示处理流程状态：待解析 → 解析中 → 向量化中 → 已完成 / 失败 |
| 文档删除 | 支持删除文档，同时清理对应的向量和图谱数据 |
| 文档更新 | 重新上传同文档覆盖更新 |
| 文档下载 | 支持下载已上传的源文件 |

> **注**：不支持文档版本管理，更新文档会覆盖旧数据。

### 3.3 智能问答

面向普通用户和管理员的核心功能模块。

#### 3.3.1 基本问答

- **多轮对话**：支持基于上下文的多轮连续对话，同一 Session 内保持对话记忆
- **知识库选择**：用户可选择单个或多个知识库作为问答范围
- **富文本回复**：支持以富文本格式展示回复，包括表格、列表、代码块、引用等
- **图表展示**：对适合图表展示的问答结果，支持思维导图、流程图等形式呈现

#### 3.3.2 来源追溯

- 每条回答中标注引用来源（所属知识库、来源文档名称、相关段落）
- 用户可点击来源文档链接查看详情

#### 3.3.3 问答反馈

- 支持对每条回答进行"有用 / 无用"投票
- 用户可填写补充反馈意见
- 反馈数据用于后续问答效果优化

### 3.4 问答历史

#### 3.4.1 功能列表

| 功能 | 详细描述 |
|------|---------|
| 历史列表 | 按时间倒序展示用户的历史对话记录 |
| 关键词搜索 | 支持按提问内容关键词搜索历史对话 |
| 时间筛选 | 支持按日期范围筛选 |
| 查看详情 | 查看单次对话的完整问答记录（包含多轮交互） |
| 导出记录 | 支持导出选中的历史对话记录（CSV/JSON 格式） |
| 删除记录 | 支持单条或批量删除历史对话 |

### 3.5 知识图谱管理

管理员对知识图谱进行可视化管理。

#### 3.5.1 功能列表

| 功能 | 详细描述 |
|------|---------|
| 图谱可视化 | 以图形化方式展示知识图谱的实体节点与关系边，支持拖拽、缩放 |
| 节点/关系编辑 | 支持新增、修改、删除实体节点与关系 |
| 图谱搜索 | 按实体名称搜索定位至图谱中的对应节点 |
| 手动触发构建 | 管理员可手动触发图谱构建/更新流程，重新抽取实体与关系 |

### 3.6 用户管理

管理员对系统用户进行统一管理。

#### 3.6.1 功能列表

| 功能 | 详细描述 |
|------|---------|
| 用户列表 | 展示所有用户信息（用户名、角色、状态、创建时间、最后登录时间） |
| 新增用户 | 创建新用户，填写用户名、密码、角色等信息 |
| 编辑用户 | 修改用户基本信息与角色 |
| 删除用户 | 删除指定用户 |
| 角色分配 | 为用户分配管理员 / 普通用户角色 |
| 知识库授权 | 按用户/用户组授权可访问的知识库 |
| 用户状态管理 | 启用 / 禁用用户账号 |
| 批量导入/导出 | 通过 Excel 模板批量导入用户，支持导出用户列表 |

### 3.7 系统配置

管理员对系统运行参数进行配置管理。

#### 3.7.1 配置项列表

| 配置类别 | 配置项 |
|---------|--------|
| LLM 参数 | 模型选择（默认 qwen3.7-plus）、温度参数（Temperature）、最大 Token 数 |
| Embedding 模型 | 模型选择（默认 text-embedding-v3） |
| 解析策略 | Chunk 分块策略（分块大小、重叠大小）、解析语言（中文/英文/自动） |
| 问答参数 | 引用来源数量（Top-K）、相似度检索阈值、是否启用重排序 |
| 检索配置 | 检索方式（向量检索 / 关键词检索 / 混合检索）、召回数量（Top-K） |
| 存储配置 | 文档存储路径、向量库路径 |
| 知识图谱配置 | 图谱构建策略（自动/手动）、实体抽取范围、关系抽取深度 |

---

## 4. 业务流程

### 4.1 知识库构建流程

```
管理员上传文档 → 文档解析（pdfplumber / python-docx） → 文本分块
    ├→ Embedding 向量化（text-embedding-v3） → 存入 ChromaDB 向量库
    └→ KG 实体关系抽取（LLM-based IE + spaCy/HanLP） → 存入 Neo4j 图数据库
```

### 4.2 智能问答流程

```
用户输入问题 → 选择知识库 → Embedding 向量化查询
    ├→ ChromaDB 向量检索（相似度匹配）
    ├→ Neo4j 图检索（关联实体）
    └→ 综合结果 → LLM（qwen3.7-plus）生成回答 → 返回富文本结果（含来源标注）
```

---

## 5. 技术架构

### 5.1 整体架构

| 层级 | 技术选型 |
|------|---------|
| 前端 | Vue 3 + Element Plus |
| 后端 | Python 3.11.3 + FastAPI |
| 向量数据库 | ChromaDB 0.4.24 |
| 图数据库 | Neo4j 3.5.5 |
| 关系数据库 | MySQL |
| 大语言模型 | 通义千问 qwen3.7-plus |
| Embedding 模型 | 通义 text-embedding-v3 |
| KG 抽取框架 | LLM-based IE + spaCy / HanLP（辅助） |
| 文档解析 | pdfplumber（PDF）+ python-docx（Word） |
| 文件存储 | 本地存储 |

### 5.2 架构说明

- 前后端分离架构，后端提供 RESTful API
- 采用混合检索策略（向量检索 + 图检索）提升召回质量
- 文档解析后同步写入向量库和图数据库，保证数据一致性

---

## 6. 非功能需求

### 6.1 性能需求

| 指标 | 要求 |
|------|------|
| 页面首屏加载时间 | ≤ 3 秒 |
| 问答响应时间 | 普通问题 ≤ 8 秒（含 LLM 推理时间） |
| 文档解析处理 | 100 页以内 PDF ≤ 30 秒完成解析+入库 |
| 并发支持 | 支持 ≥ 50 用户同时在线问答 |
| 数据查询 | 历史问答列表查询 ≤ 2 秒 |

### 6.2 安全需求

- **访问控制**：基于角色的权限控制（RBAC），未授权用户无法访问对应功能
- **认证机制**：用户登录需验证身份，支持 JWT Token 鉴权
- **数据加密**：用户密码加密存储（bcrypt 或同等强度算法），传输层启用 HTTPS
- **审计日志**：记录管理员的敏感操作（用户变更、系统配置修改等）
- **防注入**：过滤 SQL 注入、XSS 攻击等常见 Web 安全风险

### 6.3 兼容性需求

- **浏览器兼容**：支持 Chrome 90+、Edge 90+、Firefox 90+ 等主流浏览器最新两个大版本
- **响应式适配**：适配桌面端常见分辨率（1920×1080、1440×900、1366×768）

### 6.4 可扩展性需求

- **多知识库扩展**：支持新增知识库无上限，不影响系统性能
- **水平扩展**：后端服务支持横向扩展，可通过负载均衡提升并发能力
- **模型可替换**：LLM 和 Embedding 模型支持配置切换，降低对单一模型的依赖
- **插件化检索**：检索方式支持按需扩展（如加入全文检索 Elasticsearch）

---

## 7. 数据要求

### 7.1 MySQL 核心数据表

| 数据表 | 说明 |
|--------|------|
| 用户表 | 系统用户信息（用户名、密码Hash、角色、状态等） |
| 知识库表 | 知识库基础信息（名称、描述、创建时间等） |
| 文档表 | 文档元信息（文件名、路径、大小、状态、标签、所属知识库等） |
| 问答历史表 | 用户问答记录（用户ID、知识库ID、问题、回答、反馈等） |
| 操作日志表 | 管理员操作审计日志 |

### 7.2 向量数据（ChromaDB）

- 文档分块后的向量数据及对应的文本片段
- 每个向量关联来源文档 ID 和所属知识库 ID

### 7.3 图数据（Neo4j）

- 实体节点（如概念、人名、组织、产品等）
- 关系边（实体之间的语义关联）
- 实体与来源文档的关联关系

---

## 8. 附录

### 8.1 术语表

| 术语 | 说明 |
|------|------|
| RAG | Retrieval-Augmented Generation，检索增强生成 |
| Embedding | 文本向量化表示 |
| Chunk | 文档分块策略，将长文本切成语义完整的片段 |
| KG | Knowledge Graph，知识图谱 |
| LLM | Large Language Model，大语言模型 |
| RBAC | Role-Based Access Control，基于角色的权限控制 |
